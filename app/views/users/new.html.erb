<h1>Register</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit "Register", class: "btn btn-primary" %>
    <% end %>
  </div>
</div><!--/.row -->
